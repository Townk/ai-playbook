## portable

A fake project demonstrating `project_bound` playbooks, environment variable declarations,
and the `$PROJECT_ROOT` export. Used in chapter 06.

- `data/` — a placeholder data directory (the `file=` and shell blocks reference it via
  `$PROJECT_ROOT/data/…`).
